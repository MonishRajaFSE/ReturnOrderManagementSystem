apiVersion: v1
kind: Service
metadata:
    name: "returnordermanagementsystem-2867"
    labels:
        app: "returnordermanagementsystem-2867"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "returnordermanagementsystem-2867"