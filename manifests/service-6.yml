apiVersion: v1
kind: Service
metadata:
    name: "returnordermanagementsystem-0575"
    labels:
        app: "returnordermanagementsystem-0575"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "returnordermanagementsystem-0575"