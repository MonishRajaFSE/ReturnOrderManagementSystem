apiVersion: v1
kind: Service
metadata:
    name: "returnordermanagementsystem-325a"
    labels:
        app: "returnordermanagementsystem-325a"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "returnordermanagementsystem-325a"