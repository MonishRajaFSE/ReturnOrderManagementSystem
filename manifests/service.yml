apiVersion: v1
kind: Service
metadata:
    name: "returnordermanagementsystem-d2fd"
    labels:
        app: "returnordermanagementsystem-d2fd"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "returnordermanagementsystem-d2fd"