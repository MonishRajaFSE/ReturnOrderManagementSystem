apiVersion: v1
kind: Service
metadata:
    name: "returnordermanagementsystem-f827"
    labels:
        app: "returnordermanagementsystem-f827"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "returnordermanagementsystem-f827"